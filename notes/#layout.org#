#+TITLE: Layout

* Process

#+begin_src
FOR ('left', 'right', 'x', 'size'):
        matches = (True, False, True)

        ...
        elif matches == (True, False, True):
                start = layout.get_type('left')         [1]: 'fraction'
                end = layout.get_type('end')            [2]: ''

[1]:    layout.get_type('left')
        layout.left = 0
        if val is None: ...
        elif val is 'frame': ...
        elif val is 'intrinsic': ...
        elif val is TYPEOF int or TYPEOF float:
                if val >= 1 return 'constant'
                else return 'fraction'
#+end_src
